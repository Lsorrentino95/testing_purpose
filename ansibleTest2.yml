---
- name: Obtener la configuración de red
  hosts: all
  
  tasks:
    - name: Ejecutar ifconfig
      command: ifconfig
      register: ifconfig_output

    - name: Mostrar la salida de ifconfig
      debug:
        msg: "Resultado {{ ifconfig_output.content }}"
